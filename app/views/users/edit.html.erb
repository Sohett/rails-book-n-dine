<div class="container">
  <h3>Edit your credentials here</h3>
  <%= link_to 'Back', user_path(current_user) %>
  <%= render 'form' %>
</div>

<div class="container text-center" style="margin: 30px 0">
  <%= render 'owner' %>
</div>
